[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm install", "npx prisma generate", "npm run build"]

# NO ejecutar migraciones durante el build/release
# Las migraciones se ejecutan en el start cuando DATABASE_URL está disponible
[phases.release]
cmds = []

[start]
cmd = "node scripts/start-railway.js"

